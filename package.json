{
  "name": "securetechav-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix design_nine && npm run build --prefix design_nine && npx shx rm -rf dist && npx shx cp -r design_nine/dist dist"
  }
}
